apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: roboshop-claim
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: roboshop-sc
  resources:
    requests:
      storage: 4Gi
---
apiVersion: v1
kind: Pod
metadata:
  name: app
spec:
  containers:
  - name: nginx
    image: nginx:latest
    volumeMounts:
    - name: html-data
      mountPath: /usr/share/nginx/html
  nodeSelector:
    topology.kubernetes.io/zone: us-east-1b
  volumes:
  - name: html-data
    persistentVolumeClaim:
      claimName: roboshop-claim